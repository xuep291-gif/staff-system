# Installation

From the repository root (or any directory), run:

```bash
node cli/skills/scripts/install.js
```

Use paths relative to the `eav-rust` project root as needed. All configuration and agent instructions are in [`SKILL.md`](SKILL.md).
