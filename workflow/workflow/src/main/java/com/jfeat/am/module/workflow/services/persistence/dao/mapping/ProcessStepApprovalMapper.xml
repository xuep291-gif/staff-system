<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jfeat.am.module.workflow.services.persistence.dao.ProcessStepApprovalMapper">

    <insert id="insertBatch">
        INSERT INTO wf_process_step_approval (step_id, sort_num, role_id, position_id, user_id)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.stepId}, #{item.sortNum}, #{item.roleId}, #{item.positionId}, #{item.userId})
        </foreach>
    </insert>

</mapper>