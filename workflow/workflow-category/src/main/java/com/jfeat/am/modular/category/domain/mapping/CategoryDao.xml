<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jfeat.am.modular.category.domain.dao.CategoryDao">

    <select id="findRootCategories"  resultType="Category">
        SELECT *
        FROM t_category
        WHERE type_id = #{typeId}
        <if test="name != null and name!= ''">
            AND name like CONCAT('%',#{name},'%')
        </if>
        <if test="code != null and code!= ''">
            AND code like CONCAT('%',#{code},'%')
        </if>
        <if test="orgId != null">
            And org_id = #{orgId}
        </if>
        AND pid is null
    </select>


    <select id="findCategoryWithParentName" resultType="com.jfeat.am.modular.category.transfer.CategoryVO">
        SELECT c.*, p.name AS pName
        FROM t_category c
        LEFT JOIN t_category p ON c.pid = p.id
        WHERE c.type_id = #{typeId}
    </select>
</mapper>
